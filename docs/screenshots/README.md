# Screenshots

`marketlens-workflow.png` is captured from the local production frontend against
a fresh local backend with an empty SQLite history. It contains no exchange filing,
API response, secret, or user data.
